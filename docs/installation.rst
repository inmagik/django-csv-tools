============
Installation
============

At the command line::

    $ easy_install django-csv-tools

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-csv-tools
    $ pip install django-csv-tools
